version: '3.3'

services:
  api:
    image: timoa/app-stores-prometheus-exporter:latest@sha256:d8ce5a1e5008663df0eb4c5688710093db39bddf9b5ca84d7d2dc390710bc6f4
    environment:
      - NODE_ENV=production
    volumes:
      - type: bind
        source: ./config # You need to copy the config files example to the config folder at the root the project
        target: /opt/app/config
    ports:
      - 9514:9514
    restart: always
